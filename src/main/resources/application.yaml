
spring:
  application:
    name: todo
  datasource:
    url: jdbc:postgresql://localhost:5432/todo
    username: myuser
    password: secret

logging:
  level:
    org:
      springframework:
        jdbc: DEBUG
